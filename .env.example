NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=fivestep
DB_PASSWORD=fivestep
DB_NAME=fivestep

# Session token lifetime and the threshold for extending it on activity
SESSION_TTL_DAYS=30
SESSION_RENEW_AFTER_DAYS=5
